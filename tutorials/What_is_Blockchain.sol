//SPDX-License-Identifier: MIT
pragma solidity >= 0.7.0 < 0.9.0;

// Blockchain is in it's a basic form is an audible database. It's out back end. It is database which is storing our transactions.
// It is a database which data can be only added. WE do not remove or change those transaction

// Blockchain is a series of block that is chained together and that's the database which we build our applications upon.