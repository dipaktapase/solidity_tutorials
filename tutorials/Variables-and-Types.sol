pragma solidity >= 0.7.0 < 0.9.0;

// variables are just noting but reserved memory location

contract learnVariables {
    uint chocolateBar = 10;
    uint storeOwner = 300;
    bool lieDectector = true;
    string errorMessageText = "Error! that has been a mistake!";
// Exercise 
    uint wallet = 500;
    bool spend = false;
    string notifySpend = "You have spent money";


}

